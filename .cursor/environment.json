{
  "name": "Slide XL",
  "install": "if [ -f package.json ]; then npm ci; else echo 'No package.json on this checkout; skipping dependency install'; fi",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ]
}
